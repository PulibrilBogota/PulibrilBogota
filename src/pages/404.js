import * as React from "react";
import Layout from "../components/Layout";

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>NO ENCONTRAMOS LO QUE BUSCAS</h1>
      <p>Prueba con alguno de los elementos del Menú.</p>
    </div>
  </Layout>
);

export default NotFoundPage;
